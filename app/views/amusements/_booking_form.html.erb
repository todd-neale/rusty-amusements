<%= simple_form_for [@amusement, @booking] do |f| %>
  <%= f.input :start_date %>
  <%= f.input :end_date %>
  <%= f.button :submit, value: "Check Availability", class: "btn btn-primary" %>
<% end %>
